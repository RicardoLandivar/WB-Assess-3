<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    {% extends "base.html.njk" %}
    <title>{% block title %}Most Liked Fossils{% endblock %}</title>
</head>
<body>
    {% block content %}
    <h1>Welcome, {{ userName }}!</h1>
    <form action="/like-fossil" method="POST">
        <label for="fossil">Select a fossil to like:</label>
        <select name="fossil" id="fossil">
            {% for key, fossil in fossils %}
                <option value="{{ key }}">{{ fossil.name }}</option>
            {% endfor %}
        </select>
        <button type="submit">Like</button>
    </form>
    {% for key, fossil in fossils %}
        <div class="fossil-div">
            <h3>{{ fossil.name }}</h3>
            <h2>Liked {{ fossil.num_likes }} times.</h2>
            <img src="{{ fossil.img }}" />
        </div>
    {% endfor %} 
    {% endblock %}
</body>
</html>
